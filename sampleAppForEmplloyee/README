This is your new Play application
=================================

This file will be packaged with your application, when using `activator dist`.

instal postgres.
Default user postgres will be created. Alter the password of the user postgres using 
ALTER USER postgres WITH PASSWORD 'postgre';
Do create a database as test using CREATE DATABASE as postgres user
To do any postgres options on postgre attempt to do it as postgres user.
su -postgre
Then psql to create table, database and drop them. 
do /h to get all list of commands.

